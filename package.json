{
  "name": "kino",
  "version": "1.0.2",
  "private": true,
  "scripts": {
    "dev": "nuxt dev --host",
    "start": "npm run build && npm run preview -- --host",
    "build": "nuxt build",
    "preview": "nuxt preview"
  },
  "dependencies": {
    "nuxt": "^3.16.0",
    "@vite-pwa/nuxt": "^0.10.6"
  }
}
